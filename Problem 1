#include<stdio.h>
int main(){
	int i,n;
	int factorial=1;
	
	printf("enter a number :");
	scanf("%d",&n);
	
	for(i=1 ; i<=n ; i++){
		
		 
    if(factorial > INT_MAX / i) {
        printf("Overflow");
        return 0;
	}
	
}
