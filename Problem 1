#include<stdio.h>
int main(){
	int i,n;
	int factorial=1;
	
	printf("enter a number :");
	scanf("%d",&n);
	
	for(i=1 ; i<=n ; i++){
		
		 
    if(factorial > UINT_MAX / i) {
        printf("Overflow occurred! Factorial cannot be stored in 32-bit integer.\n");
        return 0;
	}
	
}
